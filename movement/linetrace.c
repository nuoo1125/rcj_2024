//いったん日本語で書く
//割り込みを設定するやつ参考俺のタクトスイッチ
int main(){
  while(gin=false){
    checkline();
    if (green = true){
      // 車の状態を管理するやつをtypedef enumで実装するからよろ
      if(state=left)move_left();
      if(state=right)move_right();
      if(state=turn)move_turn();
  }
    else{
    linetrace();
    }
}
